
# application.yml

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

# Stripe Configuration
stripe:
  apiKey: sk_test_YourTestKey
  webhookSecret: YourWebhookSecretKey
